Checklists (MVP)

- `selected.yaml` defines which checklist is active and which items are completed.
- Templates:
  - `api.yaml`
  - `saas.yaml`
  - `automation.yaml`

Validation:
- `scripts/validate_checklist.sh` ensures all template items are listed in `selected.yaml`.
